{
  "name": "agent-memory-benchmark",
  "version": "0.1.0",
  "private": true,
  "description": "ClickBench-style, reproducible benchmark for agent long-term memory systems.",
  "type": "module",
  "scripts": {
    "bench": "bun run scripts/run.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0",
    "bun-types": "^1.1.0"
  }
}
